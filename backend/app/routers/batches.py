# TODO: Admin endpoints
# Routes for batch registration and adding checkpoints to the ledger.
# POST /batches/         — create a new honey batch
# POST /batches/{id}/checkpoints — add a supply-chain checkpoint
